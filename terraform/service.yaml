apiVersion: v1
kind: Service
metadata:
  name: cost-backend-service
spec:
  type: LoadBalancer # Provisions an IP address dynamically to route dashboard API traffic
  ports:
    - port: 80
      targetPort: 5000
  selector:
    app: cost-backend
